<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core">
    <Dialog
        id="createVisitDialog"
        title="Create New Visit">
        <content>
            <VBox class="sapUiMediumMarginBeginEnd">
               
                <Label class="sapUiSmallMarginTop" text="Visit Date" required="true"/>
                <DateTimePicker id="visitDate" />
                
                <Label class="sapUiSmallMarginTop" text="Status"/>
                <Select id="status" items="{/Status}" >
                    <core:Item key="{ID}" text="{name}"/>
                </Select>
                
                <Label class="sapUiSmallMarginTop" text="Contact" required="true"/>
                <Input id="contact" />
                
                <Label class="sapUiSmallMarginTop" text="Purpose" required="true"/>
                <Input id="purpose"/>
                
                <Label class="sapUiSmallMarginTop" text="Location"/>
                <Select id="location" items="{/Location}">
                    <core:Item key="{ID}" text="{name}" />
                </Select>
                
                <Label class="sapUiSmallMarginTop" text="Description" required="true"/>
                <TextArea id="description"  />
                 <Label class="sapUiSmallMarginTop" text="Spaces" />
                <Select id="spaces" items="{/Spaces}" >
                    <core:Item key="{ID}" text="{name}" />
                </Select>
            </VBox>
        </content>
        <beginButton>
            <Button text="Create" press="onCreateVisit" />
        </beginButton>
        <endButton>
            <Button text="Cancel" press="onCancelDialog" />
        </endButton>
    </Dialog>
</core:FragmentDefinition>