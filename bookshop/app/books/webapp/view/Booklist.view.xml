<mvc:View
    controllerName="ns.books.controller.Booklist"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:l="sap.ui.layout"
>
    <Panel class="myPanel">
        <headerToolbar>
            <Toolbar
                id="idToolbar"
                width="100%"
            >
                <Title
                    id="idVisitListTitle"
                    text="Visit List"
                />
                <ToolbarSpacer />

                <SearchField
                    id="idSearchField"
                    width="50%"
                    search=".onSearchFieldSearch"
                    ariaLabelledBy="searchFieldLabel"
                    ariaDescribedBy="searchFieldDescription"
                    placeholder="{i18n>searchFieldPlaceHolder}"
                />
            </Toolbar>
        </headerToolbar>


        <Table
            id="idVisitsTable"
            width="auto"
            items="{/Visits}"
            class="SapUiResponsiveMargin"
        >
            <columns>
                <Column>
                    <Text text="{i18n>ColumnIdText}" />
                </Column>
                <Column>
                    <Text text="{i18n>VisitorNameText}" />
                </Column>
                <Column>
                    <Text text="{i18n>emailText}" />
                </Column>
                <Column>
                    <Text text="{i18n>contactText}" />
                </Column>
                <Column>
                    <Text text="{i18n>purposeText}" />
                </Column>
                                <Column>
                    <Text text="{i18n>locationText}" />
                </Column>

            </columns>
            <items>
                <ColumnListItem
                    type="Navigation"
                    press="onColumnListItemPress"
                >
                    <cells>
                        <Text text="{ID}" />
                        <Text text="{visitorName}" />
                        <Text text="{visitDate}" />
                        <Text text="{email}" />
                        <Text text="{contact}" />
                        <Text text="{purpose}" />
                        <Text text="{location}" />
                    </cells>
                </ColumnListItem>
            </items>
        </Table>
    </Panel>
</mvc:View>
